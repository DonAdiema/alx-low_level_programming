#!/bin/bash
# This script runs a C file through the preprocessor and saves the result in 'c'

# Check if $CFILE is set
if [ -z "$CFILE" ]; then
  echo "Error: \$CFILE variable is not set."
  exit 1
fi

# Run the preprocessor and save the result in 'c'
gcc -E "$CFILE" -o c
